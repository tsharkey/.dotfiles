# git
alias ghb="gh browse"

# zsh
alias rld="source ~/.zshrc"
alias ohmyzsh="code ~/.oh-my-zsh"

# utility
alias uuid="uuidgen | tr '[:upper:]' '[:lower:]' | xargs echo -n | pbcopy"

# vscode projects
alias df="code ~/dev/.dotfiles"
alias fw="code ~/dev/sportsdeck/web-app"
alias fa="code ~/dev/sportsdeck/api"

# misc
alias fixdbeaver='codesign --force --deep --sign - /Applications/DBeaverEE.app'

